#!/bin/bash
set -e

echo "🔨 Running pre-push hook: npm run build in /client"
cd client || exit 1

npm run build

if [ $? -eq 0 ]; then
  echo "✅ Build successful! Proceeding with push..."
else
  echo "❌ Build failed! Push cancelled."
  exit 1
fi
